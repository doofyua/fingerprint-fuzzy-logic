using System;
using System.Collections.Generic;
using System.Drawing;

namespace Nfiq
{
    public static partial class Nfiq
    {
        //internal static void gray2bin(int thresh, int less_pix, int greater_pix,
        //      ref int[,] bdata, int iw, int ih)
        //{
        //    for (int i = 0; i < iw; i++)
        //    {
        //        for (int j = 0; j < ih; j++)
        //        {
        //            bdata[i, j] = bdata[i, j] >= thresh ? greater_pix : less_pix;
        //        }
        //    }
        //}
    }
}
